/** App release version — single source for UI badges (Navbar, Footer). */
export const APP_VERSION = '0.2.0';

/** User-facing label shown in chrome. */
export const APP_VERSION_LABEL = `Beta · v${APP_VERSION.split('.').slice(0, 2).join('.')}`;
